<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="payment_provider_form_rotessa" model="ir.ui.view">
        <field name="name">Rotessa Provider Form</field>
        <field name="model">payment.provider</field>
        <field name="inherit_id" ref="payment.payment_provider_form"/>
        <field name="arch" type="xml">
            <group name="provider_credentials" position="inside">
                <group invisible="code != 'rotessa'" name="rotessa_credentials">
                    <!--                    <field name="rotessa_api_key" password="True" attrs="{'required':[('code', '=', 'rotessa'), ('state', '!=', 'disabled')]}"/>-->
                    <field name="test_transaction_schedule_id"/>
                </group>
            </group></field>
    </record>
</odoo>
